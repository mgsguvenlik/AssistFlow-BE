# Goal

AssistFlow CRM altında legacy Tahsilat Takibi ve sözleşmeleri, mevcut müşteri alışkanlıklarını koruyarak güvenli, hızlı ve geliştirilebilir biçimde taşımak. İlk faz tahsilat takibi/işlemleri ve veri aktarımıdır; zam ve kapsamlı raporlama sonraki fazdır. Production hazır sonucu ancak veri mutabakatı, kullanıcı kabulü, yetki ve yük testleri tamamlanınca verilir.

Bu dosya ana geliştirme görev kaydıdır. Ana agent işleri bağımlılık sırasıyla yürütür; parça sonlarında tekrar devam onayı istemez. Güncelleme: 20 Eylül 2026. Güncel müşteri kararları: [20 Eylül veri aktarım cevapları](accounting-decisions-2026-09-20.md). Çelişen eski teknik varsayımlardan önceliklidir.

# Requirements

- FE React/TypeScript/Vite, BE mevcut Model/Data/Business/WebAPI katmanları korunur. Mevcut ortak API, repository/UnitOfWork, audit, CDN, UI, hata ve store yapıları kullanılır. DI yalnız AutofacBusinessModule içinde.
- Yalnız module ait yeni tablolar/tanımlar `collection` şemasında. Mevcut dbo.Customers, ServiceType, CustomerType, CurrencyType ve diğer ortak tanımlar çoğaltılmaz. Ortak tablolarda değişiklik için kullanıcı onayı gerekir.
- Tenant yetkisi/ilişkisi/filtresi yok; kimlik doğrulama ve menü/işlem yetkileri zorunlu. Contract.ServiceTypeId mevcut ServiceType'a gider; Product bağı yok.
- SubscriberNo → SubscriberCode iş anahtarıdır. Çift/eksik eşleşme otomatik seçilmez; müşteri otomatik yaratılmaz. Eski sözleşmeler ve ilişkili geçmiş kayıpsız, tekrar çalıştırılabilir aktarılır.
- Bireysel/grup davranışları korunur. CustomerType N/GM/A/G eşlemesi; BANKA mevcut kaydı bozulmaz. ServiceType BAKIM→mevcut Bakım; diğer isimlerde onaylı map, fuzzy otomatik birleştirme yok.
- Donuk ve ücretsiz dönemde yeni borç yok; aktifleşmeden sonra tam dönem ücret, donuk döneme geriye borç yok. Önceki borçlar/ödemeler silinmez. Yeni başlangıç gerçek gün, aynı-gün yenileme, kıst yok; legacy bilinmeyen gün uydurulmaz.
- Zam geçmiş borç/ödemeyi değiştirmez. Çalışma kararı: dönem içi zam ek borç yaratmaz, sonraki yenilemede uygulanır. Fiyat yürürlük tarihi ile yenileme ankrajı ayrıdır.
- Kısa ayda son gün, sonraki ay asıl güne dönüş. Yeniden aktifleşme yeni ankrajdır. 1/2/3/4/6/12/24/36 ay desteklenir; üç aylık tutar üç ayda bir tam tutardır.
- VAR tahsilata dahildir; YOK sözleşme yok anlamındadır ve aktarıma/tahsilata dahil edilmez. Müşterinin 20 Eylül cevabına göre boş ve `Belirtilmemiş` durumlu sözleşmeler de sözleşmesi var kabul edilerek tahsilata dahil edilir; bu değerler YOK sayılmaz. Kart bilgileri ve online POS entegrasyonu mevcut kapsam dışıdır. Tarihsel ödeme yöntemi eşlemesi veri kaybını önlemek için korunur. Ücretsiz yöntem/tarihçe otomatik eşdeğer sayılmaz.
- Manuel finansal düzeltmede fiziksel silme; ters kayıt yok. Yetki, transaction, audit, concurrency ve retry kontrolü zorunlu. Grup operasyonel etiketi finansal borçtan ayrıdır.
- Kullanıcı metinlerinin tamamı Türkçe. Teknik exception/SQL/credential kullanıcıya sızmaz.
- Server pagination/sort/filter, sınırlı projection, stabil sıra, cancellation. Sorgula öncesi liste isteği yok; detay/sekme/özet ihtiyaç anında. Büyük veri performansı ölçülür, varsayılmaz.
- Her iki repo `tahsilat-module`; başka branch'e merge yok. Mevcut değişiklikler korunur; production/legacy DB'ye yazma, seed/migration çalıştırma, dosya silme gibi riskli operasyonlar ayrıca onaya tabidir.

# Completed

- **20 Eylül — yeni müşteri kararlarının uygulanması ve ikinci aktarım:** Boş/`Belirtilmemiş` 1.030 sözleşmenin kapsam issue'ları plan hash'i `16E689AC7B58DBD6C1AF43F5A1FF1469E5CAC39EB279E8377A8830E32DB86369` ile kapatıldı; yalnız kapsam engeli bulunan 734 sözleşme Pending yapıldı, başka istisnası bulunan 296 kayıt Blocked kaldı. Hem SubscriberNo hem müşteri adı olmayan 547 sözleşme ve bağlı 527 tarife dönemi plan hash'i `E819A9F35E91D3B848B4085DDD2A16E86EDA4E1DDCA8822A0E9784939C4B307B` ile Excluded yapıldı; 758 issue audit notuyla kapatıldı. Ardından genel plan hash'i `B5D191ED41DCCFEE08755162B3572844E92D43DCD913AFA1DF1D88789740FDB7` ile 734 sözleşme ve 2.256 tarife dönemi AssistFlowTest'e aktarıldı. Mutabakat başarılı: sözleşme map/stage/hedef 3.173, tarife map/stage/hedef 11.865. Toplamlar: TRY Billable 11.763/7.332.954,83; TRY Free 14/3.845,00; TRY Suspended 1/52,00; USD Billable 87/16.543,74. Ödeme ve fiziksel dosya oluşturulmadı. Importer build 0 hata/0 uyarıdır.

- **20 Eylül — müşteri e-posta kararlarının plana alınması:** Boş/`Belirtilmemiş` durumlu sözleşmelerin tahsilata dahil edilmesi; hem abone numarası hem müşteri adı bulunmayan eşleştirme kayıtlarının dikkate alınmaması; fiziksel sözleşme dosyasına ulaşılamayan sözleşmelerin dosyasız aktarılıp eksik dosyaların ayrıca raporlanması onaylandı. 2, 3, 5, 6 ve 7. maddeler için hazırlanan kayıt listeleri müşteriye iletildi; satır bazlı kararları bekleniyor. Bu kayıtlar müşteri dönüşüne kadar otomatik eşleştirilmez veya tahminle düzeltilmez.

- **20 Eylül — sekiz maddelik müşteri karar çalışma kitabı:** Müşteriye iletilen sekiz sorunun her biri için ilgili legacy/staging kayıtları tek Excel dosyasında ayrı sayfalara alındı. Sayfalar sırasıyla sözleşme durumu (1.030), müşteri türü uyuşmazlığı (1.945), AssistFlow'da bulunamayan müşteri (1.009), müşteri eşleştirme istisnaları (2.013), null işlem türü (23), tarife çakışması/güncel tutar uyuşmazlığı (1.104), para birimi-tutar istisnası (218) ve sözleşme dosya metadata adaylarını (1.584) içerir. Müşterinin dolduracağı karar, düzeltme ve açıklama alanları sarı ve seçim doğrulamalıdır; teknik alanlar ayrı tutuldu. Dosya SHA-256: `B47F94231758E908DF0CE42FF9896F0214F8D783B2BB9A6A90EB417FEFA8E523`; sekiz sayfa görsel kontrol edildi, formül hata taraması temizdir. Kaynak legacy veri ve AssistFlow hedef kayıtları değiştirilmedi.

- **20 Eylül — aktarılmış sözleşmeler için legacy dosya envanteri:** İlk planın null kontrolünün 809 boş metin çiftini metadata var saydığı tespit edilip sınıflandırma düzeltildi. Applied 2.439 sözleşmenin 1.584'ünde dolu dosya adı/yolu, 809'unda boş metadata vardır. Dolu adayların 1.572'si PDF/JPG/JPEG uzantısıyla mevcut politikaya uygun, 12'si `BOŞ.txt` olduğu için desteklenmez. Dolu kayıtlar 1.096 benzersiz legacy yoludur; 459 yol birden fazla sözleşmede kullanılır ve bu gruplar 947 satırdır. Tüm dolu yollar `~/Content/Contract/...` yapısına uygundur. Kaynak kod ZIP'i ve mevcut disklerde fiziksel dosya kökü bulunmadığından dosya/CDN yazması yapılmadı. Git dışı envanter SHA-256: `D0525C042EE8851B2B10E0BACD01929A81245C97CB3E3349461EDF3A5C18664C`; araç build'i 0 hata/0 uyarı.

- **20 Eylül — tarife para birimi/tutar Excel mutabakat dosyası:** Açık `CURRENCY_MAP_MISSING` ve `AMOUNT_INVALID` issue'ları 218 tekil tarife tarihçesinde birleştirildi. 218 kaydın tamamında para birimi belirlenemiyor; 7'sinde ayrıca tutar geçersiz ve bu 7 kayıt aynı kümenin içindedir. Legacy para birimi değeri 114 kayıtta boş, 104 kayıtta kaynak `15` değeridir. Müşteriye gönderilecek çalışma kitabında sözleşme/tarihçe/müşteri/abone bilgileri, dönem, ham para birimi ve tutar, işlem türü ve Türkçe sorun açıklaması bulunur; müşteri kararı, doğru para birimi, diğer para birimi, doğru dönem tutarı ve açıklama alanları sarıdır ve seçim doğrulaması içerir. Kaynak/staging değiştirilmedi. Çalışma kitabı SHA-256: `A4B9039AC91F4DD9A5B470F8612D6689C3C3BE6E3A2E58ABF85B7381852F158F`; formül hata taraması temiz ve iki sayfanın görsel kontrolü başarılı.

- **20 Eylül — müşteri eşleştirme Excel mutabakat dosyası:** Abone numarası boş, kaynakta tekrarlı, legacy müşteri bağlantısı bulunamayan veya AssistFlow'da aynı SubscriberCode ile hedefi olmayan kayıtlar müşteri kullanımına uygun tek çalışma kitabında birleştirildi. 2.013 tekil sözleşme; legacy sözleşme/müşteri kimliği, müşteri adı, abone numarası, bulunan hedef kimliği ve Türkçe sorun açıklamasıyla listelendi. Müşteri kararı için doğrulamalı seçenekler ve doğru SubscriberCode/AssistFlow müşteri ID/ad/açıklama giriş alanları eklendi; düzenlenecek alanlar sarı gösterildi. Açıklama sayfasında adetler ve doldurma adımları bulunur. Kaynak CSV ve snapshot değişmedi. Çalışma kitabı SHA-256: `4546EB21233C7A8DCA82FFC60A29B76DC87DD446083815C8285CA55D78F8D832`; formül hata taraması temiz ve iki sayfanın görsel kontrolü başarılı.

- **20 Eylül — müşteri eşleşme/tür mutabakat raporu:** Kalan açık müşteri issue'ları sözleşme bazında birleştirilerek Git dışındaki kesit alanına 3.958 tekil kayıtlık CSV üretildi (3.959 satır, SHA-256 `C3B0BA749F4E7E6F5154DA4D0F85FE140B7C02307DB0BE6F16159A7FA980AF9C`). Dağılım: müşteri türü uyuşmazlığı 1.945, hedef müşteri yok 1.009, kaynak müşteri orphan 543, tekrarlı SubscriberNo 323, boş SubscriberNo 138, müşteri türü map eksik 76. Tür uyuşmazlığının 1.937'si beklenen GM/Grup Üyesi → mevcut N/Bireysel farkıdır; ayrıca GM→BANKA 6, G→N 1, N→BANKA 1 vardır. Ortak `dbo.Customer` değiştirilmedi. Rapor aracı 0 hata/0 uyarıyla derlendi; rapor Git ignore kapsamında doğrulandı.

- **20 Eylül — YOK sözleşmelerin kontrollü dışlanması ve istisna sınıflandırması:** Sabit gerçek kesitte onaylı kaynak durum→aktif `NONE/YOK` eşlemesiyle 2.139 sözleşme ve bağlı 8.655 tarife dönemi tespit edildi. Salt-okuma plan SHA-256 `CC36C14B7A228D8C322B499FAD77F455A507CC6D5DB0529AE73CB3B24D42A6B6` tekrar doğrulandıktan sonra yalnız AssistFlowTest staging üzerinde Serializable transaction ve uygulama kilidiyle `Excluded` yapıldı; 2.857 açık issue karar notuyla audit geçmişi korunarak kapatıldı. Hedef finansal kayıt ve legacy yazması yoktur. Son mutabakat: sözleşme Blocked=4.920/Applied=2.439/Excluded=2.139; tarihçe Pending=13.810/Blocked=2.376/Applied=9.609/Excluded=8.655. Kalan istisnalar müşteri eşleşmesi, müşteri türü, kapsam, tarife dönem bütünlüğü, referans ve veri kalitesi olarak raporlandı. Araç build'i 0 hata/0 uyarı. Ayrıntı: [aktarım istisna sınıflandırması](collection-migration-exception-classification.md).

- **20 Eylül — Tahsilat HTTP metodu ve ortak bildirim standardı:** Tahsilat controller'larındaki kimlik, grup durumu, ödeme düzenleme ve fiziksel silme komutları mevcut proje kararı uyarınca yalnız `POST` kullanacak şekilde düzenlendi; tahsilat controller'larında `PUT`, `PATCH` ve `DELETE` bırakılmadı. FE servis yolları yeni POST uçlarıyla eşleştirildi. Tahsilat ekranlarındaki işlem, doğrulama ve veri yükleme başarı/hata bildirimleri mevcut `toast.push` + `Notification` yapısında tek ortak yardımcı üzerinden Türkçe gösterilecek şekilde birleştirildi; tekrar deneme eylemleri korundu. İlgili FE ESLint ve production build başarılı; BE solution build 0 hata (mevcut 726 uyarı).

- **19 Eylül — ilk gerçek sözleşme/tarife aktarımı:** Dry-run hash'i `F9197A0B94CCCF44657ED3D1BFB31DAA4C4E9561800EE7F1B3B2E810010E4E7E` ile kilitlenen `apply` komutu yalnız AssistFlowTest'te Serializable transaction, uygulama kilidi ve kaynak→hedef migration map'leriyle çalıştırıldı. İlk yavaş deneme commit öncesi durdurulup toplu insert'e çevrildi; transaction rollback sayesinde yarım veri kalmadı. Optimize çalıştırma 2.439 `collection.Contract` ve 9.609 `collection.ContractRatePeriod` kaydını başarıyla oluşturdu. Bağımsız `reconcile` kontrolünde map/stage/hedef adetleri birebir eşleşti; TRY Billable 9.554/6.642.753,91, TRY Free 9/2.987,00 ve USD Billable 46/12.291,68 dry-run ile aynı çıktı. Ödeme ve dosya oluşturulmadı; MGS kaynağı değiştirilmedi. Blocked satırlar staging'de inceleme için kaldı.
- **19 Eylül — gerçek kesit dry-run aktarım planı:** Salt-okuma `Collections.Import plan` komutu eklendi; manifest hash'i, batch durumu, mevcut migration map'leri, sözleşme/hedef referansları ve tüm bağlı Pending tarihçelerin eksiksizliğini kontrol eder. Sabit uzunluklu legacy `ContractID` alanlarındaki son boşluklar kanonik sayısal kimliğe normalize edildi. Gerçek kesitte 2.439 aday sözleşmenin tamamı, 1.521 müşteri, 13 servis tipi ve 9.609 tarife tarihçesiyle tam küme oluşturdu. İlk null-bazlı ölçüm 2.393 sözleşmede metadata alanı bulunduğunu raporladı; 20 Eylül envanteri bunun 809'unun boş metin olduğunu ve gerçek dolu aday sayısının 1.584 olduğunu düzeltti. Plan hash'i `F9197A0B94CCCF44657ED3D1BFB31DAA4C4E9561800EE7F1B3B2E810010E4E7E`. Tarife mutabakatı: TRY Billable 9.554 satır/6.642.753,91, TRY Free 9 satır/2.987,00 ve USD Billable 46 satır/12.291,68. Hiçbir sözleşme, tarife veya ödeme oluşturulmadı; staging durumu değişmedi.
- **19 Eylül — gerçek kesit referans hazırlığı ve son doğrulama:** AssistFlowTest'te kullanıcı onaylı matrisle 19 aktif ServiceType hazırlandı; mevcut 4/5/6 kimlikleri korunarak CustomerType kodları N/G yapıldı, GM `Grup Üyesi` ve A `Cari/Fatura` eklendi. 19 servis ve 4 müşteri tipi gerçek hedef Id'leriyle snapshot batch'ine bağlandı. Son doğrulama sonunda sözleşmeler Pending=2.439/Blocked=7.059, tarihçeler Pending=31.955/Blocked=2.495 oldu; tanım kaynaklı ServiceType, PaymentFrequency, PaymentMethod, ContractStatus ve SubscriptionStatus map eksikleri kapandı. En büyük süreç istisnasının 5.248 satırı yalnız Türkçe karakteri eksik `Fiyat Artisi`, 93 satırı açıkça `TURKCELL GPRS ZAM YANSITMASI`, 24 satırı literal `null` olarak profillendi; ilk iki değer mevcut fiyat artışı/billable semantiğine alındı, yalnız 24 literal `null` süreç karantinada kaldı. Kalan blokajlar gerçek veri/uyumluluk istisnalarıdır; gerçek sözleşme/ödeme yüklenmedi.
- **19 Eylül — gerçek kesite onaylı referans eşlemeleri:** `mgs-20260919-131119Z` batch'ine kullanıcı/muhasebe kararları kanıt metniyle birlikte 28 açık eşleme kaydedildi: ödeme dönemleri, ödeme yöntemleri, aktif/donuk, VAR/YOK/belirtilmemiş, TRY/USD/EUR ve mevcut N/G müşteri tipi karşılıkları. Kişisel CRM kullanıcı kimliği uydurulmadı; offline migration kararı nullable actor ve zorunlu `approvalReference` ile audit edildi. Eşleme girdisi snapshot klasöründe Git dışında tutulur. MGS kaynağına yazılmadı ve gerçek sözleşme oluşturulmadı.
- **19 Eylül — büyük veri yeniden doğrulama iyileştirmesi:** validator önceki açık issue'ları izlemeli toplu güncelleyip yeni issue'ları 1.000 satırlık dilimlerde kaydedecek şekilde düzenlendi; SQL komut süresi 180 saniyeye çıkarıldı. Importer temiz build oldu. İlk tekrar çalıştırma hedef DB sorgu timeout'u, ikincisi VPN/SQL taşıma bağlantısı kesilmesi nedeniyle tamamlanmadı; 28 map kalıcıdır, staging issue özeti son başarılı validasyon durumunu göstermeye devam eder.
- **19 Eylül — gerçek MGS sözleşme kesiti ve test staging:** Kullanıcının MGS'nin artık aktif kullanılmadığını doğrulamasıyla kaynakta hiçbir değişiklik yapmadan tek Serializable salt-okuma transaction kullanıldı. Kesit `mgs-20260919-131119Z`: 19.906 Customer, 9.498 Contract ve 34.450 ContractHistory; SHA-256 manifest `F61D471E7E7F1CCF8BAD923AE4BC8AA15F1EA09DC71D4CF01C8A7C7472244263`. Inspect/profile başarılıdır. 63.854 ham satır yalnız AssistFlowTest `collection` staging tablolarına idempotent kaydedildi; gerçek Contract/RatePeriod/Payment oluşturulmadı. İlk validate tamamlandı ve referans map'leri henüz uygulanmadığı için 9.498 sözleşme ile 34.450 tarihçenin tamamı karantinada kaldı. Profilde 5.365 beklenmeyen tarihçe süreç değeri, 116 boş ve bir `900` para birimi ile 3.267 tam dosya metadata çifti görüldü. Sonraki adım map eksiklerini gerçek veri kalitesi sorunlarından ayırıp onaylı referans eşlemelerini uygulamaktır.

- **19 Eylül — gerçek legacy sözleşme kesiti aracı ve kaynak tutarlılık kontrolü:** `tools/Collections.Snapshot`, bağlantıyı yalnız ortam değişkeninden alarak `Core.Customer`, `Core.Contract` ve `Core.ContractHistory` tablolarını tek Snapshot transaction içinde NDJSON'a aktarmak üzere eklendi. Yalnız MGS veritabanını kabul eder, salt-okuma ApplicationIntent kullanır, dolu klasörü ezmez ve kişisel veri içeren çıktıyı Git dışında tutar. Araç temiz build oldu. VPN üzerinden gerçek kaynak bağlantısı kuruldu; canlı MGS'de Snapshot Isolation'ın kapalı olduğu SQL 3952 ile doğrulandı. Canlı ayar değiştirilmedi, kilit yaratabilecek Serializable kesit alınmadı ve yarım boş çıktı temizlendi. Sonraki veri provası DBA tarafından aynı ana ait restore/kopya sağlanınca inspect/profile/stage/validate sırasıyla yapılacaktır.

- **19 Eylül — legacy Toplu Zam ekranı:** Eski `Pages/Core/Raise` akışı CRM → Tahsilat → Toplu Zam altında yenilendi. Kullanıcı rapor listesinden en fazla 50 ücretli sözleşmeyi açıkça seçer ve zam oranı veya sabit yeni dönem fiyatından yalnız birini uygular; sabit fiyat farklı para birimlerine topluca uygulanamaz. Her sözleşme mevcut rowversion kontrollü tarife değiştirme servisiyle ayrı işlenir, başarısız kayıt diğerlerini durdurmaz ve Türkçe sonuç özeti gösterilir. Mevcut para birimi/ödeme dönemi korunur; geçmiş borç ve tahsilatlar yeniden yazılmaz. Legacy ekranın görünür filtre listesinin tamamına kontrolsüz işlem uygulaması ve `RunRaise` geçmiş ödeme güncellemesi taşınmadı. Yeni tablo/migration/backend yazma yolu eklenmedi. BE Release build 0 hata (mevcut 792 uyarı), ilgili FE lint ve production build başarılı; gerçek kullanıcı kabulü açık.

- **19 Eylül — legacy Müşteri Sözleşme Raporu taşıması:** Eski `Pages/Report/CustomerContract` ekranının halen kullanılan sözleşme, müşteri, durum ve tarife bilgileri `GET /api/collections/reports/contracts` ile CRM → Tahsilat → Sözleşme Raporu ekranına taşındı. Abone/müşteri/GTS/IVR araması, sıralama ve 25/50/100 server-side sayfalama SQL'de uygulanır; yalnız sorgu verildiğinde istek atılır. Güncel tarihli tarife tutarı, para birimi, ödeme dönemi ve ücretsiz hizmet bilgisi gösterilir; detay eylemi mevcut `/contracts/:id` sayfasına gider. Legacy kart/banka alanları ve ağır 12-ay ödeme kolonları müşteri kararı ile yeni rapora alınmadı; ödeme hareketleri ayrı Ödeme Raporunda kalır. Yeni tablo/migration yoktur, DI mevcut AutofacBusinessModule üzerindedir. BE Release build 0 hata (mevcut 792 uyarı), ilgili FE lint ve production build başarılı; gerçek HTTP/tarayıcı kabulü açık.

- **19 Eylül — legacy Ödeme Raporu ilk taşıma dilimi:** Eski `Pages/Report/PaymentReport` kaynak kodundaki tarih aralığı, abone/müşteri araması ve salt-okuma ödeme listesi yeni sisteme taşındı. `GET /api/collections/reports/payments` mevcut CollectionFollowUp/View ve CollectionRead kapısıyla korunur; tarih filtresi, arama, count, sıralama ve 25/50/100 server-side sayfalama SQL'de uygulanır. FE ekranı CRM → Tahsilat → Ödeme Raporu altında yer alır ve Sorgula öncesi istek atmaz. Legacy kart/banka/POS alanları müşteri kararı gereği gösterilmedi; yeni süreç veya DB tablosu eklenmedi. AutofacBusinessModule kullanıldı. BE Release build 0 hata (mevcut 794 uyarı), ilgili FE lint ve production build başarılı; gerçek HTTP/tarayıcı kabulü açık.

- **19 Eylül — yeni sözleşme tam sayfa akışı ve route kabulü:** “Yeni sözleşme” dialogu kaldırılarak `/crm/collections/contracts/new` sayfasına taşındı; Geri/Vazgeç sözleşme listesine, başarılı kayıt yeni `/contracts/:id` detayına gider. Formun mevcut aramalı seçimleri, idempotency anahtarı, doğrulamaları ve Türkçe mesajları korundu. Gerçek oturumda yeni sözleşme sayfası, sözleşme detayı `/contracts/97`, dönem durumu `/tracking/97?period=2026-09-01` ve her üçünün listeye Geri dönüşü salt-okuma olarak doğrulandı. İlgili ESLint ve FE production build başarılı; veri kaydı yapılmadı.

- **19 Eylül — dialog detaylarının route sayfalarına dönüşümü:** Sözleşme detayı `/crm/collections/contracts/:id`, grup dönem/tahsilat durumu `/crm/collections/tracking/:id?period=...` adresli bağımsız detay ekranlarına taşındı. Liste eylemleri doğrudan bu adreslere gider; her iki ekranda ana listeye dönen Geri düğmesi vardır. Sözleşmenin düzenleme, abonelik, zam, tarife geçmişi, ödeme ve CDN dosya işlevleri korunup detay sayfasına alındı. Grup durum dialogu ve eski query-param sözleşme detayı kaldırıldı. Yeni kayıt ve toplu tahsilat gibi kayıt detayı olmayan işlem pencereleri mevcut ortak dialog yapısında bırakıldı. İlgili ESLint ve FE production build başarılı.

- **17 Eylül — P13 salt-okuma kesit profili:** `Collections.Import profile <kesit klasörü>` komutu eklendi. Mevcut manifest/dosya hash ve kaynak kimliği tekilliği kontrolünden sonra yalnız müşteri türü, referans kimliği, süreç tipi ve dosya metadata var/yok adetlerini raporlar; DB bağlantısı/yazması yoktur. Kişisel alan, ham kaynak değer veya dosya yolu basılmaz; beklenmeyen metin maskelenir. Importer Release build başarılı. Gerçek kesit henüz olmadığından kaynak profil sonucu ve onaylı eşleme kararları açık.

- **17 Eylül — P11 tahsilat takip CSV dışa aktarımı:** `GET /api/collections/tracking/export` mevcut View yetkisi ve modül kapısıyla, listeyle aynı filtre/sıralama sorgusundan sayfa sınırı olmadan CSV akıtır. Sorgu tek kez kurulup `AsAsyncEnumerable` ile satır satır okunur; `ReportData`/Excel byte[] bellekte biriktirme kullanılmaz. İptal işareti veritabanı okuması/yazmaya taşınır, UTF-8 BOM ve Türkçe başlıklar bulunur, kullanıcı metinlerinde CSV/formül enjeksiyonuna karşı kaçış uygulanır. FE geçerli sorgu filtresiyle indirme yapar. BE Release build, ilgili FE lint ve FE Vite build başarılı; gerçek HTTP/büyük veri kabulü açık.

- **17 Eylül — P16 takip sorgusunda erken tarih daraltması:** Seçilen ayla kesişmeyen tarife dönemleri, `DateDiffMonth`/yenileme günü hesaplamasından önce `EffectiveFrom`, `EffectiveToExclusive` ve sözleşme bitiş tarihi ile SQL'de elenir. Vade gününün mevcut kesin kontrolü korunur; ödeme-only satırları etkilenmez. Yeni indeks veya DB değişikliği yapılmadı. BE Release build ve gerçek SQL plan/performans kabulü izleniyor.

- **17 Eylül — P06/P09 bakiye durumu filtresi:** Tahsilat takip API ve ekranına tüm kayıtlar, borcu kalanlar, borcu kalmayanlar (fazla ödeme dahil) ve yalnız ödeme kayıtları seçenekleri eklendi. Filtre bireysel satırda doğrudan, grup görünümünde grup/para birimi toplamından sonra SQL'de uygulanır; count ve sayfa aynı koşulu kullanır. Grup satırından sözleşmelere inişte üyelerin tamamı gösterilerek özetin mutabakatı korunur, dönüşte seçilen grup filtresi geri gelir. BE Release build, ilgili FE lint ve FE Vite build başarılı; gerçek SQL/HTTP kabulü açık.

- **17 Eylül — P09 takip ekranı yetki/boş durum düzeltmesi:** Tahsilat takip ekranı görüntüleme yetkisiyle okunabilir kalır; toplu tahsilat eylemi ve satır seçim kutuları yalnız mevcut `CollectionFollowUp/Edit` yetkisiyle görünür. İstemcide kayıt öncesi aynı yetki tekrar denetlenir; API yetkisi değiştirilmedi. Sorgu yapılmadan önce Türkçe başlangıç yönlendirmesi gösterilir. İlgili FE lint ve Vite build başarılı; gerçek oturum kabulü açık.

- **17 Eylül — P09 sözleşme detayına doğrudan iniş:** Tahsilat takip listesindeki sözleşme eylemi artık genel listeye değil seçilen sözleşmenin detayına gider; URL kimliğiyle detay isteğe bağlı yüklenir ve kapatıldığında kimlik URL'den kaldırılır. Bireysel/grup satırları aynı detayı kullanır, ek liste sorgusu yapılmaz. Grup özeti belge metnindeki eski “grupsuz ayrı özet” ifadesi gerçek davranışa göre düzeltildi. İlgili FE lint başarılı; gerçek görsel kabul açık.

- **17 Eylül — P05 tarife tutarı/ücretsiz geçişi:** Aktif, VAR ve tahsilata dahil sözleşmede dönem tutarı veya ücretsiz hizmet tercihi `POST /collections/contracts/{id}/rate-change` ile sürüm kontrollü değiştirilebilir. Mevcut ödeme sıklığı, para birimi ve yenileme ankrajı korunur; eski tarife aynı gün kapatılıp yeni tarife tek transaction içinde açılır. Bugün yenileme günü ise geçmiş/güncel borcu yeniden yazmamak için işlem reddedilir; diğer günlerde yeni tutar ilk sonraki yenilemeye uygulanır. Gerekçe/actor/zaman tarihçede saklanır. FE detay formu, Türkçe mesajlar ve ilgili SWR yenilemesi eklendi. Ortak tablo ya da yeni migration yok. BE Release build, FE Vite build ve ilgili FE lint başarılı; gerçek HTTP/tarayıcı kabulü ayrıca izleniyor.

- **17 Eylül — grup dönem etiketi:** Legacy `FollowGroupStatus` işleminin grup toplamı değil sözleşme+dönem kaydı olduğu kaynak koddan doğrulandı. Mevcut `collection.ContractPeriodFollowUp` üzerinde görüntüleme ve sürüm kontrollü kayıt API'si, Autofac kaydı ve isteğe bağlı FE durum penceresi eklendi. Yalnız gruba bağlı sözleşme kabul edilir; aktif yeni tanım seçilir, değişmeden korunan pasif tarihsel tanım kalabilir. Bu işlem Payment/ContractRatePeriod veya bakiyeyi değiştirmez. BE/FE build, ilgili FE lint ve değişen FE dosyalarının type-check tanıları temiz; gerçek HTTP/kullanıcı kabulü açık.

- **17 Eylül — gerçek grup detayı:** Tahsilat grup özetinde `CustomerGroupId` boş müşteriler artık sahte tek grup olarak birleştirilmez. `CustomerGroupId` sunucu filtresi SQL'de sayfalamadan önce uygulanır; FE grup/para birimi satırından filtreli sözleşme görünümüne iner ve özete döner. Böylece bireysel kayıt ve grup özetinin finansal kapsamı ayrılır. BE/FE build ve ilgili FE lint başarılı; gerçek oturumda görsel kabul açık.

- **16 Eylül — legacy ek metadata koruması:** Contract kesitindeki FileAttachmentName/FileAttachmentPath stage izdüşümüne eklendi. Tek tarafı eksik veya hedef alan sınırını aşan çiftler issue ile karantinaya alınır; ham JSON korunur. Dosyanın fiziksel CDN aktarımı yapılmadı.

- **16 Eylül — sözleşme dosyaları/CDN dilimi:** Kullanıcı mevcut CDN yapısının tüm dosyalarda değiştirilmeden kullanılmasını onayladı. `collection.ContractAttachment` metadata tablosu ve migration'ı AssistFlowTest'e uygulandı; `GET/POST api/collections/contracts/{id}/attachments` menü görüntüleme/düzenleme yetkisiyle açıldı. Listeleme server-side 20/100 sınırında, yükleme en fazla 20 MB PDF/PNG/JPG ve içerik imzası kontrollü. Dosya doğrudan mevcut `IFileStorage.SaveAsync` ile CDN'e gider; ortak dosya servisi değişmedi. FE sözleşme detayında dosya bölümü isteğe bağlı açılır, dosya listesi otomatik ön-yüklenmez. BE ve FE build başarılı, ilgili FE lint temiz; gerçek CDN yükleme kabulü kullanıcı ortamında henüz yapılmadı.

- **16 Eylül — ağ hatası Türkçeleştirme:** FE ortak `parseApiError`, response bulunmayan ağ/zaman aşımı hatasını genel `error.message` dalından önce Türkçe karşılar. Sunucu iş kuralı yanıtları korunur. Vite build başarılı; ilgili helper'da önceden mevcut genel ESLint `any`/kullanılmayan değişken hataları var, bu dilimde yeni lint hatası eklenmedi.

- **16 Eylül — referans kimliği tekrar güvenliği:** Onaylı map girdisinde sayısal kaynak kimlikleri kanonikleştirildi (`01`/`1` aynı anahtar); önceki eşlemeyle karşılaştırma da bu kurala göre yapılır. Böylece görünürde ayrı fakat aynı legacy kimliğe giden kararlar aktarım doğrulamasını bozmaz.

- **16 Eylül — takip seçim kullanılabilirliği:** FE ortak DataTable'a geriye uyumlu, opsiyonel satır seçilebilirlik kuralı eklendi. Tahsilat takip ekranında grup, borçsuz ve yalnız ödeme satırlarının checkbox'ı artık pasif; mevcut işlev korunur. İlgili ESLint ve Vite build başarılı.

- **16 Eylül — kaynak müşteri türü ve açık tarife tutarlılığı:** Legacy müşteri türünün `Core.Customer.Type` (N/GM/A/G) olduğu koddan doğrulandı; staging artık yanlış `CustomerTypeID` varsayımını kullanmaz. Her tür için onaylı eşleme ile hedef `Customer.CustomerTypeId` tutarlılığı zorunludur. Contract üzerindeki güncel tutar/kur/ödeme tipi, tek açık ContractHistory dönemiyle karşılaştırılır; fark karantinaya gider. Ham kayıt değişmez; importer derlemesi başarılı.

- **16 Eylül — legacy süreç tipi/dahil olma kontrolü:** `Customer.aspx` tarihçe seçicisindeki beş ProcessType değeri doğrulandı. `validate`, Başlangıç/Fiyat Artışı/Fiyat İndirimi→Billable, Ücretsiz→Free, Hizmet Dondurma→Suspended izdüşümünü yalnız bilinen kodlarda yapar; bilinmeyen kodu bloke eder. Sözleşme durumu yalnız EXISTS ise dahil edilir; YOK/boş/UNKNOWN ve eşlemesiz/geçersiz abonelik durumu blocker olur. Geçmişten donma tarihi uydurulmaz ve hiçbir satır henüz `Ready`/gerçek finansal tabloya geçmez. Importer derlemesi başarılı.

- **16 Eylül — tarihçe dönem/tutar doğrulaması:** Legacy `Customer.aspx.cs` içinde PaymentMethodID ile PaymentTypeID'nin ayrı alanlar olduğu ve ContractHistory'nin ödeme tipi/tutar/para birimi taşıdığı kontrol edildi. `validate`, PaymentTypeID için yalnız onaylı PaymentFrequency eşlemesini kabul eder; tarihçe tutarını hedef decimal(18,2)'ye yuvarlamasız sığma açısından denetler. Eksik/geçersiz değer ayrı blocker issue ve karantina sebebidir. Ham satır korunur; gerçek veri yüklenmedi.

- **16 Eylül — referans doğrulama entegrasyonu:** Staging `validate`, kabul edilmiş ServiceType/CurrencyType map'lerini gerçek sözleşme/tarihçe satırlarına uygular; CustomerTypeID mevcutsa hedef müşteri türünü, dolu sözleşme/abonelik durumu ve ödeme yöntemi alanlarını kontrol eder. Eksikler kalıcı blocker issue ve `Blocked` durumuna dönüşür; yeniden doğrulama çözülen issue'yu kapatır. Dönem alanının kaynak anlamı ve finansal uygunluk henüz doğrulanmadığı için `Ready`/gerçek yükleme açılmadı. Importer derlemesi başarılı; gerçek kesit olmadığı için veri provası yapılmadı.

- **16 Eylül — açık referans eşlemesi:** `Collections.Import map` komutu yalnız AssistFlowTest'teki önceden staging'e alınmış kesit için onaylı kaynak ID → hedef ID kararlarını kaydeder. ServiceType, CurrencyType, CustomerType ve dört `collection` tanımı desteklenir. Hedefin mevcut ad/kodu doğrulanır; silinmiş servis tipi reddedilir; çelişkili tekrar değiştirilmez. `TargetCustomerTypeId` için collection FK/check migration'ı test DB'ye uygulandı. Ortak `dbo` verisi değiştirilmedi; gerçek kaynak kesiti/eşleme dosyası henüz sağlanmadığından hiçbir eşleme satırı yazılmadı. BE derlemesi başarılı.

- **16 Eylül — staging müşteri/tarihçe karantina doğrulaması:** `tools/Collections.Import validate` sabit kesit hash/sayılarını doğrular; SubscriberNo→SubscriberCode tam eşleşmesi, kaynak/hedef tekilliği ve silinmiş hedef kontrolü yapar. History parent/customer bağı, tarih geçerliliği, aynı başlangıç, iç içe dönem dahil overlap, çoklu açık dönem ve history'siz sözleşme ayrı açık issue kodlarıyla izlenir. Legacy dahilî bitiş günü stage'de hariç üst sınıra çevrilir. Çözülen issue geçmişi korunur; engelli satır Blocked, diğerleri eşlemeler bitene dek Pending kalır. 500 satırlık güncelleme dilimleri ve tekrar çalıştırma yolu eklendi. Gerçek kaynak kesiti olmadığı için SQL veri kabulü henüz yapılmadı; importer Release build başarılı.

- **16 Eylül — sabit kesit staging komutu:** `tools/Collections.Import` ile üç önceden dışa aktarılmış NDJSON dosyası (`Customer`, `Contract`, `ContractHistory`) ve sürümlü manifestin salt-dosya doğrulaması eklendi. `stage` yalnız AssistFlowTest'e yazar; ham JSON/hash ve sözleşme/tarihçe Pending izdüşümü 500 satırlık dilimlerde kaydedilir. Aynı kesit/hash tekrarında duplicate yok; değişmiş kaynak satırı ezilmez. Legacy veya ortak dbo'ya bağlantı/yazma yok. Gerçek kaynak kesiti paylaşılmadığından içe alma çalıştırılmadı. Ayrıntı: [sabit kesit içe alma](collection-migration-snapshot-import.md).

- **15 Eylül — P12 aktarım staging temeli:** yalnız `collection` şemasında MigrationBatch, MigrationSourceRow, MigrationContractStage, MigrationRatePeriodStage, MigrationReferenceMap, MigrationIssue ve MigrationMap modelleri/configuration/migration'ı oluşturuldu. Ham JSON ve SHA-256 hash, snapshot/rule sürümü, türlenmiş NoAction hedef FK'leri, rowversion, açık issue tekilliği, kaynak ve hedef idempotency indeksleri eklendi. Migration AssistFlowTest'e uygulandı; salt-okuma doğrulamasında 7 tablo ve 0 aktarım kaydı görüldü. Legacy DB'ye yazılmadı, ortak dbo değiştirilmedi ve henüz kaynak veri alınmadı. Ayrıntı: [aktarım staging temeli](collection-migration-staging.md).

- **15 Eylül — toplu tahsilat girişi:** sözleşme bazlı takip ekranında pozitif bakiyesi bulunan en fazla 50 satır seçilip tek ödeme tarihi ve açıklamayla tahsil edilebilir. Grup özetleri ve yalnız ödeme satırları işleme alınmaz. Her satır kayıt transaction'ında güncel tahakkuk/ödeme yeniden hesaplanır; bakiye değişen satır Türkçe gerekçeyle atlanır. Ana toplu işlem anahtarından deterministik alt anahtar üretilerek kısmi tamamlanmış isteğin aynı bilgilerle tekrarında çift kayıt önlenir. Mevcut Payment/PaymentOperation, CollectionFollowUp/Edit, Autofac ve ortak DataTable/Dialog/Form yapıları kullanıldı; migration/ortak dbo değişikliği yok. BE Release ve FE production build başarılı. Ayrıntı: [toplu tahsilat](collection-batch-payment-entry.md).

- **15 Eylül — sözleşme devirli bakiye:** mevcut dönem bakiye ekranına isteğe bağlı önceki dönemleri dahil et seçeneği eklendi. İlk tarife ayından seçilen ayın hesap tarihine kadar ücretli dönemler ve ödemeler para birimi bazında toplanır; sözleşme bitişinden sonra borç yoktur. Geçmiş durum güncel abonelikle yeniden yorumlanmaz. BE Release/FE build ve lint başarılı; AssistFlowTest üzerinde tek/devirli sorgular salt-okuma çalıştı. DB yazması ve yeni test senaryosu yok.

- **15 Eylül — tahsilat düzeltme ve fiziksel silme:** ödeme listesine rowversion kontrollü düzenleme ve açık onaylı fiziksel silme eklendi. Mevcut PaymentOperation audit/idempotency transaction'ı kullanılır; silme sonrası audit kalır. URL sözleşme sahipliği ve düzenlemede dönem/para birimi uygunluğu transaction içinde denetlenir. Belirsiz sonuç 503 ve aynı işlem anahtarıyla retry; kesin stale durum 409'dur. BE Release ve FE build, ilgili lint başarılı. Debug çıktı çalışan API tarafından kilitli olduğundan uygulama durdurulmadı. Yeni test/migration/DB yazması yok. Ayrıntı: [ödeme düzeltmeleri](collection-payment-corrections.md).

- **15 Eylül — grup takip ve yalnız ödeme istisnaları:** takip ekranına Sözleşme bazlı/Grup özeti seçimi eklendi. Grup görünümü mevcut CustomerGroup ve para birimi üzerinden toplamları ve sözleşme sayısını verir. Borcu olmayan fakat dönem ödemesi bulunan kayıtlar `Yalnız ödeme` olarak görünür. CustomerType/ortak dbo değiştirilmedi. Bireysel ve grup sorguları AssistFlowTest üzerinde salt-okuma çalıştı; BE/FE build ve ilgili lint başarılı. Yeni test senaryosu yazılmadı.

- **15 Eylül — takip ekranı yetki hatası düzeltildi:** FE'de mevcut olmayan `canViewMenu` çağrısı kaldırıldı; repository standardındaki `hasMenuAccess('CollectionFollowUp')` kullanıldı. İlgili lint ve Vite build başarılı.

- **15 Eylül — ana tahsilat takip listesi (sözleşme bazlı ilk dilim):** CRM/Tahsilat altına Tahsilat Takibi eklendi. Tek dönem borç/ödeme/kalan tutarı; abone-müşteri araması, müşteri/servis/para birimi filtreleri ve finansal kolon sıralamaları SQL'de server-side sayfalamadan önce uygulanır. N+1 yoktur. Backend ve FE build, ilgili lint ve AssistFlowTest salt-okuma SQL çeviri/sayfalama kontrolü başarılı; DB yazılmadı, yeni test senaryosu eklenmedi. Kapsam/sınırlar: [tahsilat takip listesi](collection-tracking-list.md).

- **14 Eylül — sözleşme dönem bakiyesi:** mevcut takvim hesabı, SQL ödeme toplamı ve para birimi bazında kalan tutar detay ekranına bağlandı. Sorgu yalnız Hesapla ile çalışır; eksik tarife sıfır sayılmaz. Yeni tahsilat/durum değişiminde eski bakiye görünümü yenilenir. Yeni tablo/DI/DB yazması yok. BE build, FE Vite build ve ilgili lint başarılı; yeni test senaryosu yazılmadı. Gerçek SQL/HTTP kabulü açık. Kapsam ve sınırlar: [dönem bakiyesi](collection-period-balance.md). Bu dilim ana tahsilat listesinin veya devirli toplam bakiyenin tamamlandığı anlamına gelmez.

- **14 Eylül — tekil tahsilat giriş dilimi:** pozitif tutarlı ödeme API/servis/formu mevcut işlem makbuzu altyapısına bağlandı. Dönem/para birimi/tarihsel ücretli tarife uygunluğu transaction içinde denetlenir. Mevcut yetki, Autofac, Form/Input/Select ve sayfalı DataTable kullanıldı. Aynı payload/anahtar replay ve belirsiz sonuçta form kilidi var. BE build ve FE Vite build başarılı; yeni test yazılmadı. Yeni finansal endpointin gerçek HTTP/SQL kabulü açık. Yeni şema/ortak tablo değişikliği yapılmadı. Kapsam ve kabul kriterleri: [tekil tahsilat girişi](collection-payment-entry.md).

- **14 Eylül — ödeme hareketlerini görüntüleme:** sözleşme detayına isteğe bağlı ödeme listesi ve `GET /api/collections/contracts/{id}/payments` okuma aksiyonu eklendi. Mevcut menü yetkisi ve okuma feature flag'i korunur. SQL üzerinde sayfalama (en fazla 100), tarih/Id sıralaması ve sınırlı alan projeksiyonu kullanılır. FE ortak DataTable/SWR üzerinden tarih, dönem, tutar, para birimi, ücretsiz bilgisi ve açıklama gösterir; bölüm kapalıyken istek atılmaz. BE build 0 hata, FE Vite build ve ilgili üç FE dosyasının ESLint kontrolü başarılı. Mevcut proje uyarıları sürüyor; Vite tam type-check değildir. Yeni test paketi/senaryosu eklenmedi; mevcut API aksiyon listesi beklentisi yeni okuma aksiyonuna uyarlandı. Ödeme oluşturma/düzeltme/silme ekranları bu dilime dahil değildir.
- **14 Eylül — tarayıcı düzenleme kabulü:** kullanıcının açtığı oturumla sözleşme detayından düzenleme, servis tipi tek select içinde arama/seçme ve IVR güncellemesinin başarı mesajıyla detaya yansıması doğrulandı. Önceki oturum bekleme notu kapanmıştır; tüm modülün uçtan uca kabulü anlamına gelmez.

- **13 Eylül — P05/P08 sözleşme referans bilgisi düzenleme tamamlandı:** servis tipi, GTS ve IVR alanları `PATCH /collections/contracts/{id}/identity` ile güncellenir. Mevcut CollectionFollowUp/Edit, yazma kapısı, Autofac scoped servis, repository ve tek transaction kullanıldı. Rowversion eski/tekrar gönderimi reddeder; değiştirilmemiş pasif tarihsel servis korunur, yeni seçim aktif mevcut dbo.ServiceType kaydı olmalıdır. Müşteri, başlangıç/bitiş, tarife ve ödeme alanları bu komutta değişmez. Güncelleyen kullanıcı ve zaman mevcut audit alanlarına yazılır; ayrı önce/sonra değişiklik günlüğü eklenmedi. FE detayındaki düzenleme formu mevcut aramalı CollectionReferenceSelect ve Form/Input/Button kullanır, form açılışındaki sürümü sabit tutar, kayıt sırasında kapatma/diğer durum işlemleri engellenir; başarılı kayıtta detay ve liste yenilenir. **44 gerçek SQL sözleşme kontrolü + 129 model/API kontrolü**, ilgili ESLint, Vite build ve değişen izlenen dosyalarda diff kontrolü başarılı. SQL fixture kayıtları temizlendi; ortak tablo/şema değişikliği yapılmadı. Tarayıcı E2E henüz doğrulanmadı. Tarihli ücret/ücretsiz/dahil olma düzenlemesi ve aynı gün durum düzeltmesi ayrı açık P05 işleridir.

- **13 Eylül — P05/P08 abonelik dondurma/aktifleştirme:** `POST /collections/contracts/{id}/subscription`, Türkçe DTO, mevcut CollectionFollowUp/Edit, kimlik denetimi ve Autofac scoped servis eklendi. Mevcut collection tabloları kullanıldı; migration gerekmedi. Serializable tek context/transaction içinde eski tarife kapanır, aynı gün başlayan yeni tarife ve sözleşme durumu birlikte kaydedilir. Eski tutar/para birimi/ödeme dönemi korunur; donuk borç üretilmez, aktifleşme yeni ankrajdır, ücretsiz ve VAR/YOK uygunluğu korunur. Rowversion eski/tekrar/eşzamanlı komutu 409 ile reddeder; commit yanıt kaybında otomatik başarı replay yerine detay yenilemesi istenir. Açıklama, actor ve zaman tarifede saklanır. FE ortak Form/Input/Button ile detayda yetkili işlem, Türkçe mesajlar ve ilgili SWR yenilemeleri tamamlandı. **37 gerçek AssistFlowTest SQL kontrolü + 124 model/API kontrolü**, ilgili ESLint ve Vite build geçti. Test kayıtları temizlendi. Gerçek tarayıcı/HTTP yetki kabulü henüz yapılmadı. Sınırlar ve sonraki bağımlılıklar `collection-subscription-changes.md` içinde; P05 bütünü tamamlanmış değildir.

- **13 Eylül — P05/P08 detay ve tarife tarihçesi bağımlılığı:** detay yanıtı sözleşme/abonelik durum adları, GTS/IVR ve rowversion içerir; liste projection'ı büyütülmedi. `GET /collections/contracts/{id}/rate-history` mevcut read servis/DI, CollectionRead ve View yetkisiyle sunucu taraflı 25/50/100 sayfalama sağlar; silinmiş sözleşme/period dışlanır, pasif tarihsel tanımlar korunur. FE detayda kullanıcı açınca yüklenen tarihçe, tutar/para birimi/dönem/geçerlilik/açıklama ve Türkçe boş/hata/tekrar deneme durumları eklendi. **119 model/sorgu/API kontrolü**, ilgili ESLint ve Vite build başarılı. Gerçek HTTP/tarayıcı kabulü açık; DB değişikliği yapılmadı. Bu dilim düzenleme/dondurma/aktifleştirme komutlarını henüz uygulamaz; sonraki P05 bağımlılığı rowversion kontrollü tarihli geçiş, transaction/audit ve finansal tarihçe testleridir.

- **13 Eylül — P08 tanım aramalarının uçtan uca tamamlanması:** sözleşme/abonelik durumu ve ödeme dönemi dahil ortak CollectionDefinitionSelect tek kutuda arama/seçim destekler. Definitions API opsiyonel, en fazla 200 karakter Search ile ad/kod filtreler; count ve sayfa aynı filtreyi kullanır. 400 ms gecikme, önceki isteğin iptali, 25 kayıtlık sayfalama ve Türkçe tekrar deneme korunur. Mevcut Select/API/DI yapıları kullanıldı; şema değişikliği gerekmiyor. FE Vite build başarılı; görsel oturum kabulü henüz yapılmadı.

- **13 Eylül — tahsilat navigasyonu ve aramalı referans seçiciler:** CRM altında gelecekteki tüm tahsilat süreçlerini barındıracak `Tahsilat` açılır grubu oluşturuldu; mevcut ekran `Sözleşmeler` adıyla ve aynı `CollectionFollowUp` yetkisiyle bu gruba taşındı. Yeni sözleşme formundaki müşteri, servis tipi ve para birimi için ayrı arama kutusu/düğmesi kaldırıldı. Mevcut ortak Select ve YKB müşteri seçimi etkileşimi kullanılarak tek kutuda seçim + 400 ms gecikmeli sunucu araması, 25 kayıtlık sayfalama, aşağı kaydırmada devam yükleme, eski istek sonucunu eleme ve Türkçe durum mesajları uygulandı. Aynı arama tekrarında menü açılışı gereksiz API isteği üretmez. İlgili ESLint, `git diff --check` ve production Vite build başarılı; bağımlılıklardan gelen mevcut Rollup/chunk uyarıları devam ediyor. Yetkili gerçek oturumda görsel kabul açık.

- Son transaction dilimi doğrulaması: **88 model/sorgu/API/DI/işlem kontrolü geçti**, BE build **0 hata / 725 uyarı**. Beş yeni test requestId, actor, payload, inactive model ve cancellation sınırlarıdır; gerçek transaction gövdesi SQL'de çalıştırılmadı. Veritabanı/seed/host açılmadı. Aktif yazma endpoint'i yok.

- P07 transaction persistence ilk dilimi: CollectionPaymentTransaction, mevcut Repository aynı AppDataContext ile kullanılarak serializable transaction içinde payment save + audit/receipt save + commit yapar. EF execution strategy delegate içinde transaction oluşturulur; rowversion conflict ve unique RequestId yarışı Türkçe conflict döndürür. Erken dönüş/hata transaction dispose ile rollback olur; kendi takip ettiği payment/receipt detach edilir. Replay işlem makbuzudur, güncel ödeme varlığı/bakiyesi iddiası değildir. DI/endpoint kaydı yok; finansal uygunluk/yetki command servisi tamamlanmadan çağrılmayacak.

- Son doğrulama (10 Eylül): **83 model/sorgu/API/DI/işlem içeriği kontrolü geçti**, backend build **0 hata / 47 uyarı**. Yeni snapshot/hash testleri gerçek transaction veya eşzamanlı request testi değildir. Önceki 76 offline kural paketi bu dilimde değiştirilmedi. DB/host/seed çalıştırılmadı.

- 10 Eylül bağımsız güvenlik dilimi: typed CollectionPaymentCommand, schemaVersion=1 canonical SHA-256 hash ve açık audit snapshot projection eklendi. Create/update/delete alanları, decimal(18,2) sınırı, muhasebe ayı, 8-byte rowversion doğrulanır. Tutar ölçeği ve kültür farkı hash'i değiştirmez; diğer alan farkları korunur, açıklama trim edilmez. RequestId anahtarı ve actor hash dışında ayrı doğrulanır. Hash istemciden alınmaz. Model kuralları DB yetkisi/finansal uygunluk anlamına gelmez; negatif değerler teknik olarak temsil edilebilir, yeni manuel işlem politikası komut servisinde belirlenecek.

- P03 audit/receipt model dilimi: `collection.PaymentOperation`, global unique RequestId, actor, işlem türü, SHA-256 için 32 byte payload hash, ödeme kimliği, önce/sonra JSON ve tamamlanma zamanı. Ortak genel idempotency/audit karşılığı bulunmadı; Helpdesk TicketHistory kendi modülüne bağlı ve ödeme silinmesi sonrası kalıcı receipt ihtiyacına uygun değil. Mevcut BaseEntity/EF konvansiyonu kullanıldı. Ödemeye FK yok, fiziksel silme receipt'i cascade silmez. Create/update/delete snapshot şekilleri ve JSON/hash yapısı için check taslağı eklendi. Bu tablo henüz aktif modelde değil.
- P03 saf replay kontrolü: aynı kullanıcı+aynı 32-byte hash tekrar yanıtına aday; farklı kullanıcı/içerik reddedilir. Yetki ayrıca her istekte kontrol edilecek. Replay eski ödeme snapshot'ını otomatik güncel sonuç gibi döndürmez; command servisi kayıtlı işlem sonucu ve mevcut silinmişlik durumunu açıkça ele alacak.

- P02 sabit kaynak tanım eşlemeleri eklendi: PaymentMethod, SubscriptionStatus, ContractStatus, GroupStatus kaynak kimliği → kod. Hedef DB kimliği uydurulmaz; bilinmeyen kimlik null, boş sözleşme durumu kaynak Id14→UNKNOWN. LEGACY_FREE yalnız kaynak ödeme yöntemi kodudur, tarihli ücretsiz davranışa dönüşüm değildir. Banka canonical map ve seed uygulaması hâlâ ayrı işler.
- P03 ilk model dilimi: collection.Payment, GroupStatus ve ContractPeriodFollowUp. Payment mevcut BaseEntity/ITimestamped/IAuditedByUser kullanır, ISoftDeletable kullanmaz; açık CurrencyType FK, decimal(18,2), nullable 1000 karakter açıklama, rowversion, nonunique dönem indeksi. Negatif eski iadeler temsil edilebilir; yeni manuel giriş yetkisi/validasyonu ayrıca yapılacak. Müşteri Contract üzerinden çözülür, Payment üzerinde ikinci müşteri kaydı yok.
- Grup takipte opsiyonel durum, kaynak kapasitesi 500 karakter açıklama, rowversion ve aktif contract/month unique indeksi; ödeme FK veya otomatik finansal aksiyon yok. Silme/grup etiket davranışı mevcut kararlar doğrultusunda ayrı komut olacaktır.
- Güncel doğrulama: **72 offline kontrol + 64 model/sorgu/API/DI kontrolü**, BE build **0 hata / 47 uyarı** (artımlı). Testte nullable kimlik method-group eşlemesi derleme hatası verdi, açık lambda ile düzeltilip tekrar geçti. Aktif context, migration ve DB değişmedi.

Kod varlığı ile entegrasyon/kabul tamamlanması farklıdır:

- Legacy kaynak, DB profilleri, ekran/işlem analizi: workspace `../docs/tahsilat/faz-0` altında 01–18 belgeler. Bu yol workspace köküne göredir; belgeler şu an BE git kapsamı dışında.
- Ayrı branch'ler, sözleşme ve PaymentFrequency taslak model/configuration, aktif model dışında metadata testleri.
- Takvim, çakışma, aday para birimi, ödeme sıklığı eşleme, izole tahakkuk hesabı; önceki son çalışmada 67 offline kontrol.
- Sözleşme read query/service/controller; ortak repository/envelope/DI, default-disabled flag ve missing-model 503. Query SQL çevirisi ve DI kontrolleri mevcut; önceki paket 42 kontrol.
- FE `/crm/collections/contracts` rotası, yetki sınırı, Sorgula/list/detail, ortak bileşenler/ApiService/parseApiError/Zustand; menü kaydı yapılmadı.
- Türkçe annotation/başarı mesajları ve collection kapsamlı model-binding filtresi eklendi; son dilimin regresyon testi güncellenecek.
- Önceki FE build/lint ve BE build başarılı. Bu, HTTP entegrasyonu, TypeScript projesinin tamamı veya yük testi başarısı değildir.
- P01 model/configuration dilimi: collection.ContractRatePeriod, BillingAnchor, aynı Model enum'u, NoAction referanslar, decimal(18,2), koşullu ücretli zorunluluk ve aktif dönem indeksleri. 67 offline + 48 model/sorgu/API/DI kontrolü geçti; build 0 hata / 47 uyarı (artımlı). Migration uygulaması yok. Kaynak açıklama profili ve aktif model entegrasyonu ayrı işlerdir.
- P02 model dilimi: Bank/PaymentMethod/SubscriptionStatus/ContractStatus collection tanımları, opsiyonel Contract FK'leri ve kart son kullanma date alanı eklendi. Seed/map/UI henüz yok; P02 tam bitmiş değildir. Aynı BaseEntity/EF configuration konvansiyonu kullanıldı, yeni ortak generic model tabanı kurulmadı. Mapping/ad normalizasyonu ayrıca doğrulanacak.
- P15 Türkçe doğrulama: framework İngilizce binding hatasının Türkçe ResponseModel'e çevrilmesi, geçerli isteğin geçmesi, annotation ve filter order kontrolleri eklendi. Test kurulumundaki ValidationContext instance hatası düzeltilip yeniden çalıştırıldı.
- Son doğrulama: **67 offline kontrol; 58 model/sorgu/API/DI kontrolü; BE build 0 hata / 47 uyarı**. Uyarılar artımlı derleme sayısıdır, baseline iyileşmesi iddiası değildir. DB bağlantısı/host/migration yok.
- P08 yenileme dilimi: FE Sorgula/table göstergesi isValidating kullanıyor; yenileme sırasında tekrar submit engelleniyor. Dosyaya özel ESLint başarılı; tarayıcı kabulü henüz yok.

# In Progress

- **20 Eylül güncel aktarım bağımlılığı:** YOK ve kimliği tamamen boş müşteri kararları uygulandı; boş/`Belirtilmemiş` durumdan güvenli bulunan ikinci küme aktarıldı. Güncel staging: sözleşme Blocked=3.639/Applied=3.173/Excluded=2.686; tarihçe Pending=11.075/Blocked=2.328/Applied=11.865/Excluded=9.182. Müşteriden 2, 3, 5, 6 ve 7. sayfalardaki satır bazlı kararlar bekleniyor; bu kümeler karantinada kalır. Güncel aktarılmış dosya envanteri 3.117 kayıt: 1.650 dolu ve yapısal olarak uygun yol, 1.467 boş metadata, 22 desteklenmeyen uzantı; fiziksel dosya/CDN yazması yapılmadı.
- **17 Eylül güncel bağımlılık:** P13 kaynak kesit profil komutu hazır; gerçek NDJSON kesiti gelmeden dağılım/istisna sonucu üretilemez. P12–14 aktarım provası için onaylı map ve fiziksel dosyalar eksik. Gerçek oturum kabulü denendi fakat açık tarayıcı oturumu giriş ekranına yönlendi; kimlik bilgisi kullanılmadı. Kod tarafındaki bağımsız P15/P16 güvenlik ve sorgu işleri sürdürülecek.

- **P11 kabul:** Akış halinde CSV uygulandı; gerçek HTTP oturumunda boş/dolu filtreler, dosya açılışı ve yüksek hacimde bellek/iptal davranışı doğrulanacak. Mevcut genel Excel/CSV exporter tüm satırları `ReportData` ve byte[] içinde topladığı için büyük tahsilat sonucu üzerinde doğrudan kullanılmadı.

- **Güncel sıra (17 Eylül):** P05 tarife/ücret-ücretsiz geçişi uygulandı; gerçek HTTP kabulü açık. Sıradaki bağımlılık P09 bireysel/grup ekranlarının gerçek oturum kabulü ve eksik legacy filtreleri. P12–14 gerçek kesit ve fiziksel dosyalar olmadan veri yükleme provasına geçmez. P10 CDN yüklemesi gerçek ortamda doğrulanacak. Aşağıdaki eski tarihli “sonraki bağımlılık” kayıtları tarihsel çalışma notudur, güncel öncelik değildir.

- **Sonraki bağımlılık (16 Eylül):** kaynak Contract/ContractHistory durum ve ProcessType değerlerinin finansal dahil olma/tarife davranışıyla kontrollü eşlenmesi; ardından yalnız tamamen doğrulanmış sözleşme-history kümelerinin izole yükleme provası. Sabit kaynak kesiti henüz sağlanmadığı için gerçek veri alınmadı.

- **Sonraki bağımlılık (15 Eylül):** rowversion/audit kontrollü ödeme düzeltme ve fiziksel silme. Grup müşteri görünümü ve borç satırı olmayan ödeme istisnaları tamamlandı; dönemler arası devir ve toplu işlem daha sonraki dilimlerdir.

- **Sonraki bağımlılık — ana tahsilat takip sorgusu:** dönem bakiyesi detay önkoşulu tamamlandı. Şimdi bireysel/grup liste satırlarının borç/ödeme bazında birleştirilmesi ve finansal filtre/sıralamanın SQL'de sayfalamadan önce uygulanması gerekir. Detay servisini satır başına çağırmak yasaktır; N+1 veya tüm kayıtları belleğe alma yapılmayacak. Ödeme düzeltme/silme ve toplu işlem ardından ilerler.

- **Geçici kayıt temizliği tamamlandı (14 Eylül):** bağlantı açıldı; Id=95 ve CreationRequestId=fb1268d3-02e1-48c5-bf72-13b7e7e3ebb6 varlığı SELECT ile doğrulandı. Korumalı temizlik aracı yalnız işaretli geçici sözleşmeyi ve sıfır tutarlı tarifelerini fiziksel sildi; ortak kayıtlara dokunulmadı.

- **Güncel öncelik (14 Eylül):** tekil tahsilat girişinin ardından ana tahsilat takip/bakiye sorgusu, ödeme düzeltme/silme ve toplu işlem entegrasyonu. Kullanıcı talebiyle ek test kapsamını büyütmeden ana geliştirmeler gerekli derleme/lint kontrolleriyle ilerletilir; nihai kullanıcı kabulü kullanıcı tarafından yapılacaktır.

- **Tarayıcı oturumu engeli kapandı:** kullanıcı giriş yaptı; sözleşme düzenleme ve kaydetme doğrulandı. Ayrıntı Completed bölümündeki 14 Eylül kaydındadır.

- **13 Eylül — açık kullanıcı onayıyla Admin açılışı:** AssistFlowTest dbo.Menus'a CollectionFollowUp (Id 111), dbo.MenuRole'a yalnız Admin/ADMIN (RoleId 1) için HasView/HasEdit eklendi ve sonuç doğrulandı. Tek transaction/hedef ve rol guard'lı `docs/sql/grant-collection-admin.sql` kaydedildi; diğer roller ve ortak iş tabloları değişmedi. Development CollectionRead.Enabled/ContractCreateEnabled true yapıldı; production ayarları değişmedi. FE CRM menüsüne izin kontrollü bağlantı eklendi. Önceki yetki/onay bekleme maddesi kapanmıştır. Çalışan API/FE sürümünün yenilenmesi ve oturum yetki önbelleğinin yeniden yüklenmesi gerekebilir; host otomatik migration/seed çalıştırdığı için bu işlem burada başlatılmadı. Gerçek Admin oturumunda tarayıcı kabulü hâlâ yapılmadı.
- **13 Eylül — sözleşme oluşturma dilimi uygulandı:** FE ortak modal/form/seçiciler, mevcut tenant'sız müşteri/servis/para birimi servisleri ve `POST /collections/contracts` bağlandı. Kimlik doğrulama + CollectionFollowUp/Edit, Autofac scoped DI, Türkçe DTO doğrulaması, tek context/repository/transaction, sözleşme+ilk tarife birlikte kayıt mevcut. `CreationRequestId`/32-byte hash nullable alanları + unique indeks + CHECK `20260913081707_AddCollectionContractCreationRequest` ile AssistFlowTest collection.Contract'a uygulandı; ortak tablo değişmedi. Aynı anahtarın farklı kullanıcı/içerikle kullanımı reddedilir; aynı içerik replay edilir. İstek anahtarı form açıkken korunur, tarayıcı depolamasına PII yazılmaz.
- **13 Eylül doğrulama:** 19 gerçek SQL oluşturma kontrolü geçti (gerçek bariyerli deadlock yarışı, rollback, commit sonrası yanıt kaybı simülasyonu, ilk tarife/gün, YOK/boş/donuk/ücretsiz/kısa sözleşme, geçersiz müşteri). Test kayıtları temizlendi. 112 model/API/migration + 91 offline kontrol geçti; BE build 0 hata/47 uyarı (artımlı). FE ilgili dosyalar lint temiz, Vite build başarılı. Genel FE tsc mevcut diğer alanlardaki hatalar nedeniyle temiz değil; yeni Collections dosyalarında tanı görülmedi. Authenticated browser E2E/görsel kabul henüz yok; production hazır denmez.
- **Kullanıma açma kapısı:** salt-okuma kontrolünde dbo.Menus içinde CollectionFollowUp sayısı 0; Development CollectionRead.Enabled/ContractCreateEnabled ayarları tanımsız, varsayılan kapalı. Ortak menü/rol yetki tablolarına müdahale etmemek için otomatik grant yapılmadı. Kullanıcının menü kaydı eklenmesine onayı ve hedef görüntüleme/düzenleme rolü gerekir; sonra feature açılması ve gerçek oturumla browser kabulü yapılabilir. Bu oluşturma diliminin kalan işi bu kapıdır. Düzenleme/dondurma/aktivasyon, ödeme command API ve ana tahsilat sorgusu ayrı plan maddeleridir.
- **12 Eylül — P05 kalıcı yenileme günü:** `ContractRatePeriod.OriginalAnchorDay` nullable tinyint ve 1–31 CHECK, `20260912184617_AddCollectionOriginalAnchorDay` ile yalnız AssistFlowTest collection tablosuna uygulandı. Mevcut alanlar/backfill değiştirilmedi; NULL eski BillingAnchor.Day davranışını korur. CollectionRateSegment opsiyonel orijinal günü takvim hesabına aktarır; ücret/para birimi davranışı değişmez. **91 offline + 104 model/API/migration kontrolü geçti.** Yeni migration tam foundation→aktif model farkıyla eşleşir; historical foundation için kapsam kontrolü korunur. Kurulum script'i DB guard/lock/transaction içerir, Down gün bilgisini sileceğinden otomatik çalıştırılmaz. Sonraki zorunlu bağımlılık: sözleşme oluşturma command'ında ilk borç tarihi/orijinal günün birlikte atanması ve persisted entity→segment projeksiyonu; henüz frontend kayıt formu veya write endpoint tamamlanmadı. CollectionRead kapısı değiştirilmedi.
- **12 Eylül — tanım seçicisi ve sözleşme filtresi:** düzenlenebilir sözleşme formu henüz bulunmadığından yeniden kullanılabilir FE `CollectionDefinitionSelect`, mevcut listeye sözleşme durumu filtresi olarak bağlandı. Ortak Select/Button kullanır; yalnız menü açılınca yükler, kaydırmada sonraki sayfayı getirir, unmount/tür değişiminde isteği iptal eder, Türkçe hata/tekrar deneme sunar. Seçim taslağı feature store'da; liste sadece Sorgula ile filtrelenir. BE opsiyonel ContractStatusId aynı count/list Filter üzerinden uygulanır; boş filtre null durumlu kayıtları dışlamaz, seçili UNKNOWN ise yalnız o tanım Id'sine bağlı kayıtları seçer (NULL ile birleştirmez). Dört ek filtre kontrolüyle **102 model/API kontrolü geçti**, BE build 0 hata/47 uyarı; değişen FE dosyaları ESLint başarılı. Tarayıcı görsel/etkileşim kabulü ve kayıt formu hâlâ açık; feature kapısı açılmadı, DB değişmedi.
- **12 Eylül — tanım okuma API/FE servis dilimi:** `GET api/collections/definitions` yalnız istenen türün aktif tanımlarını server-side pagination ile döndürür (sayfa boyutu en fazla 100; kararlı Id son sıralaması). Mevcut ResponseModel/PagedResult, UnitOfWork/Repository, MenuAuthorize ve CollectionRead kapısı kullanıldı; DI AutofacBusinessModule'da. FE ortak ApiService çağrısı AbortSignal destekli; hiçbir ekrana otomatik istek eklenmedi, form bileşenine bağlama sıradaki iştir. Pasif tarihsel ödeme yöntemleri seçim listesinden gelmez; geçmiş detayda referansları ayrıca gösterilmelidir. 98 model/API/DI kontrolü, FE build ve değişen servis ESLint başarılı. SQL sıralama çeviri hatası tür bazlı sıralamayla düzeltildi; `--read-definitions` modu sadece okur. Gerçek HTTP yetki matrisi ve tarayıcı form kabulü henüz tamamlanmadı. Veri yazma/özellik açma yapılmadı.
- **12 Eylül — P02 tanım seed/eşleştirme dilimi tamamlandı:** mevcut IDataSeed/SeedRunner ile yalnız `CollectionDefinitionSeed` çağrıldı. AssistFlowTest'te 8 ödeme dönemi, 3 sözleşme durumu, 2 abonelik durumu, 7 grup etiketi ve 10 tarihsel ödeme yöntemi olmak üzere **30 kalıcı collection tanımı** oluşturuldu. İkinci çalışma Id/ad/aktiflik/dönem dahil hiçbir kayıt değiştirmedi; 30 legacy kod karşılığı doğrulandı. Ortak tablo/legacy/menü/yetki değişikliği yok. Tarihsel ödeme yöntemleri yeni kullanım politikası belirlenene kadar pasif; bu POS entegrasyonu değildir. Diğer tanımların IsActive alanı seçilebilirliktir, borç uygunluğu değildir. Genel startup seed listesine eklenmedi; modül seed'i açık kurulum komutuyla çağrılır. 89 offline kontrol tekrar geçti. Sonraki parça: tanım lookup servisi ve sözleşme komutlarına bağlama, ardından mevcut yetki filtresiyle ödeme command entegrasyonu. P02 bütünü/production kabulü tamamlandı iddiası yok.
- **12 Eylül — P05 başlangıç takvimi dilimi:** `CollectionStartRules` yeni sözleşmeler için 1–15 mevcut ay / 16+ sonraki ay aynı gün ve yalnız EXISTS dahil kararını kodlar. Ortak takvim primitive'ine geriye uyumlu opsiyonel orijinal gün girdisi eklendi; Şubat'a clamp edilmiş ilk tarih sonraki yenilemelerin gününü kaydırmaz. **89 offline kontrol geçti** (13 yeni kontrol). Bu primitive henüz command/persistence/tarife sorgusuna bağlanmadı; ilk borç tarihi ve orijinal yenileme günü aktarımı P05/P06 entegrasyonunun açık kabul kriteridir. Dondurma/aktifleşme takvimine başlangıç kesmesi tekrar uygulanmayacak. DB veya mevcut borç değişmedi; eski legacy tarihlere otomatik uygulanmadı.
- **12 Eylül — dev senkronizasyonu:** kullanıcı talebiyle BE `dev` 434c8a9 ve FE `dev` 4358dc4, kendi `tahsilat-module` branch'lerine çakışmasız birleştirildi. Fetch sonrası local dev/origin/dev eşitliği doğrulandı. BE solution build 0 hata / 801 uyarı, 76 offline + 94 model kontrolü ve FE Vite build başarılı. FE değişen dosyalarda ESLint 269 hata / 15 uyarı verdi; bu dosyaların tümü dev ile birebir aynı olduğundan merge kaynaklı yeni fark değil. Kapsam dışı lint refactoring yapılmadı. Vite build tam TypeScript kontrolü değildir; DB/SQL testleri bu merge için çalıştırılmadı. Ters yönde merge veya push yapılmadı.
- **11 Eylül iş kararı güncellemesi:** kullanıcı VAR=dahil / YOK=hariç ilişkisini açıkladı ve kalan sınırlar için teknik karar yetkisi verdi. Tam 15 mevcut ay; boş/UNKNOWN yeni borç üretiminden hariç; 16+ başlangıçta ilk borç sonraki ay aynı gün, orijinal gün ankrajı korunur. Ayrıntı ve müşteriye iletilecek metin `accounting-decisions-2026-09-11.md`. Önceki “muhasebe cevabı bekleniyor” notları bu konularda geçersizdir; kod/test entegrasyonu P05/P06/P15 görevlerine dahildir. Bu karar kaydı DB veya geçmiş borçları değiştirmedi.
- **11 Eylül son doğrulama:** çift/sahipsiz ödeme sayımı dahil **20 gerçek SQL kontrolünün tamamı geçti**; son test çalışmasının kayıtları temizlendi. Aşağıdaki 19 kontrol ve tekrar bekleme notu ara çalışma kaydıdır; tekrar tamamlandı. `git diff --check` başarılı. Bu dilimde üretim kodu değiştirilmedi, test projesi derlenip gerçek DB'de çalıştırıldı.
- **11 Eylül — P07 SQL kabul dilimi:** yeni `tools/Collections.Sql.Tests` gerçek AssistFlowTest'te çalıştırıldı. İlk 19 kontrol başarılı: rollback, rowversion, aynı/farklı payload ve kullanıcı, bariyerle eşzamanlı yazma, commit sonrası yanıt kaybı simülasyonunda retry, silme ve eski create replay. Teste ait sözleşme/ödemeler/makbuzlar finally ile temizlendi; ortak tablolara veya legacy'ye yazılmadı. Identity sayaçları normal olarak ilerledi. Son ek ödeme sayımı kontrolüyle toplam 20 kontrolün tekrar çalıştırılması izleniyor. Bu sonuç HTTP yetki/finansal eligibility veya gerçek ağ failover kabulü değildir. Sıradaki bağımsız iş: tanım seed/map ve command authorization entegrasyonu; muhasebe belirsizlikleri etkinleştirilmez.
- **Güncel durum — P04 temel kurulum tamamlandı:** `20260910150310_AddCollectionFoundation` AssistFlowTest üzerinde tek transaction ile uygulandı. 10 collection tablosu, 21 ikincil indeks, 12 NoAction FK, 14 etkin/güvenilir CHECK doğrulandı. Bağımsız bağlantıda tabloların tamamı boş ve migration kaydı mevcut. Aktif AppDataContext artık collection modellerini içeriyor; önceki “model kaydı yok/DDL yok” notları tarihsel durumdur. Ortak tablolar ve önceki migration kayıtları değişmedi. Seed, legacy aktarımı ve finansal endpoint aktivasyonu yapılmadı.
- Doğrulama: **94 model/sorgu/API/migration kontrolü + 76 offline iş kuralı kontrolü başarılı; solution build 0 hata / 47 uyarı** (artımlı). Migration Up SQL'i modül-only model farkıyla birebir eşit; target model aktif modelle uyumlu. Sonraki bağımlılık: gerçek SQL transaction/rowversion/idempotency kabul testleri ve tanım seed/map hazırlığı. Açık muhasebe kuralları hâlâ aktive edilmez.
- VPN sonrası salt-okuma preflight başarılı: hedef `AssistFlowTest`, `collection` tabloları henüz yok; CREATE TABLE/CREATE SCHEMA yetkileri mevcut. `dbo.Customers`, `dbo.ServiceType`, `dbo.CurrencyType` Id alanları bigint NOT NULL. Ağ engeli kalktı; önceki erişim hatası tarihsel kayıttır. Henüz DDL/veri yazılmadı.
- Migration geçmişi farkı: DB'de olup yerel migration dosyalarında olmayan kayıtlar `20251124200930_FixCustomerDuplicateColumn`, `20260619194553_addPriorityDefionationTable`, `20260705204551_addWorkOrderToArchive`, `20260905071247_AddServiceRequestServiceTypes`. Yerelde olup DB'de eksik kayıt saptanmadı. Bu kayıtlar silinmeyecek/değiştirilmeyecek; körlemesine database update veya ortak şema düzeltmesi uygulanmayacak. Sıradaki P04: collection kapsamlı migration/script üretimi, gerçek hedef FK/şema önkoşulu denetimi ve kontrollü kurulum.
- Son kullanıcı yetkisi: AssistFlow hedefinde tahsilata özel gerekli tablolar `collection` altında oluşturulabilir. Legacy yazma, ortak dbo tablo/veri değişiklikleri ve production veri aktarımı bu yetkinin dışında kalır. Development yapılandırmasının hedefi `192.168.1.8 / AssistFlowTest`; ilk salt-okuma bağlantı kontrolü erişim hatası verdi. Fiziksel tablo ve migration geçmişi henüz doğrulanamadı; DDL uygulanmadı.
- P04 güvenlik kontrolü eklendi: EF relational model farkının tamamı yalnız `collection` EnsureSchema/CreateTable/CreateIndex işlemlerinden oluşmalı; tam 10 tablo, cascade olmayan FK ve transaction dışı komut bulunmaması test edildi. **92 model/sorgu/API kontrolü geçti**. Bu sonuç gerçek hedef şema uyumluluğu veya uygulanmış migration anlamına gelmez.
- Transaction kodunun gerçek SQL rollback/commit-belirsizliği/retry/yarış/rowversion testleri izole DB kurulumu onayına bağlı. Offline guard testleri bunların yerine geçmez. Sonraki kod işi aynı transaction içinde müşteri/sözleşme/para birimi/dönem uygunluğu ve command authorization entegrasyonu; belirsiz başlangıç/VAR-YOK finansal eligibility kuralları aktive edilmez.

- 10 Eylül: başlangıç/VAR-YOK açık maddeleri korunarak audit/idempotency geliştirmesi sürdürüldü. Ödeme ve makbuzun aynı transaction'da uygulanması sonraki dilim; doğrudan write endpoint veya DI kaydı eklenmedi. Snapshot serializer yalnız ödeme alanlarını içerir, navigation veya ham request serialize etmez. Güncel 15'i kesme kuralı belirsizken borç üretimi aktive edilmeyecek.

- 10 Eylül cevaplarıyla kart/banka taslağı çıkarıldı. 15'i kesme kuralında tam 15 ve ilk borç/yenileme günü ayrımı, VAR/YOK/boş dahil olma matrisi netleşmeli. P05/P06 başlangıç/dahil olma hesabı bu açıklamaya bağımlıdır; bağımsız audit/idempotency işleri devam edebilir.

- P00 repository/karar denetimi ve FE bağımsız incelemesi tamamlandı; görev kaydı oluşturuldu.
- Sıradaki parça P03 typed komut içeriğinin kanonik hash üretimi ve P07 ödeme/receipt/audit tek transaction servis taslağı. Unique indeks yarışı, rollback ve fiziksel silme retry testi gerçek izole DB kabulüne dahil. Audit modeli tek başına immutability veya exactly-once garantisi sağlamaz. P02 banka canonical map, seed taslağı ve kaynak uzunluk kontrolleri açık.

# Todo

Her satırın bitişi kanıt/test ile kaydedilir. Bağımlılık yoksa güvenli bağımsız iş paralel yürütülebilir.

| ID | İş / bağımlılık | Kabul kriteri |
| --- | --- | --- |
| P00 | Denetim ve plan | Gerçek git durumu, eksikler, test sınırları, eski karar çelişkileri kayıtlı |
| P01 | Tarife modeli / onaylı kurallar | collection şeması, doğru FK/decimal/date, ankraj ayrımı, overlap guard, model testleri |
| P02 | Sözleşme tanımları / P01 | PaymentMethod/Status/Bank mevcut karşılıkları tekrar kontrol edilmiş; ortak tablo değişmeden kayıpsız alanlar, bank canonical map |
| P03 | Finansal ödeme/grup takip/audit/idempotency modelleri / P01 | Fiziksel silme güvenliği, currency ve dönem bağı, concurrency, cascade yok, unique işlem anahtarı, Türkçe hata |
| P04 | Tamamlandı: aktif EF temel modeli ve kontrollü AssistFlowTest kurulumu / P01–03 | Yalnız collection DDL; migration SQL/model eşitliği, 10 tablo/21 indeks/12 FK/14 CHECK ve boş veri doğrulandı; runbook docs/collection-foundation-installation.md |
| P05 | Sözleşme CRUD/tarife/dondurma/aktifleşme / P01–04 | Açık komutlar, geleceğe etkili değişiklik, tarihçe korunur, tek transaction/concurrency, kayıtlı borç yeniden yazılmaz |
| P06 | Tahsilat takip sorgu API / P03–05 | N/grup görünümü, tüm legacy gerekli filtreler, currency bazlı toplam, aynı filtre count/list/export, Türkçe hatalar |
| P07 | Tek/toplu ödeme ve düzeltme / P03–06 | Kısmi ödeme, retry çift kayıt yok, stale tutar kontrolü, fiziksel silme audit, yetkisiz komut reddi, etiket bağımsız |
| P08 | FE sözleşme ekranını tamamla / P02,05 | Ortak lookup/çeviri/form/state, cancellation, erişilebilirlik, listeye dönüş, eşzamanlı değişiklik mesajı, görsel kabul |
| P09 | FE bireysel ve grup tahsilat akışları / P06–07 | Bireysel sözleşme/dönem kaydı; yalnız gerçek grup özeti; grup→üyelerin sözleşmelerine iniş/geri dönüş; grup operasyonel etiketlerinin borçtan ayrılması; lazy detay, gereksiz istek yok, Türkçe yüklenme/boş/hata durumları ve görsel kabul |
| P10 | Dosya/CDN / P03,05 | Mevcut IFileStorage; sözleşme/dosya erişimi, metadata pagination, boyut/tür kontrolü, eski dosya doğrulaması |
| P11 | Export/isteğe bağlı özet / P06 | Ortak export/job varsa kullan; sayfa değil tüm filtre sonucu, sınırlı bellek, kullanıcı erişimi ve iptal |
| P12 | Migration staging/map/istisna kodu / P01–04 | Kaynak audit ve ham değer kayıpsız; hash/idempotency, duplicate müşteri/kur/overlap karantina; silineni retry yaratmaz |
| P13 | Salt-okuma eksik profil / P12 | Currency aday/view farkı, tekil issue sayısı, collation, müşteri silinmişlik, açıklama uzunluğu, dosya/bağlı kayıt profili |
| P14 | İzole aktarım provası / P12–13 + ortam onayı | Sabit veri kesiti, contract/history/payment/grup sıra; currency bazlı tutar ve sayım mutabakatı, rollback provası |
| P15 | HTTP yetki/güvenlik/Türkçe regresyon / P04–11 | Anonim/izinsiz/okuma/yazma matrisi; bağlama/validation/exception Türkçe; veri sızıntısı yok; startup seed çalıştırmadan test host |
| P16 | Performans / P06,09,14 | Temsilî büyük veri, query plan/index, p95 gecikme/bellek/DB çağrı sayısı, derin sayfa ve eşzamanlılık; hedefler ölçümle somutlaştırılır |
| P17 | Production geçiş / P14–16 + onay | İş birimi kabulü, erişim/backup/rollback/izleme/runbook, modül açık ayarı ve veri yükleme ayrıca onaylı |
| P18 | Sonraki faz zam/rapor | İlk faz kabulünden sonra mevcut legacy iş kapsamı; geçmiş ödeme değişmez |

# Decisions

- 20 Eylül müşteri e-posta kararı: boş veya `Belirtilmemiş` sözleşme durumu, sözleşmenin bulunmadığı anlamına gelmez; bu kayıtlar tahsilata dahil edilir. Yalnız açık `YOK` sözleşme yok anlamındadır ve dışarıda kalır. Bu değişiklik önce staging üzerinde plan/hash ve bağlı tarihçe bütünlüğü doğrulanarak uygulanacaktır.
- Müşteri eşleştirme istisnalarında hem abone numarası hem müşteri adı olmayan kayıtlar işleme alınmaz. Alanlardan en az biri varsa otomatik dışlama yapılmaz; müşterinin satır bazlı eşleştirme kararı beklenir.
- Legacy dosya metadata'sı olup fiziksel dosyası bulunamayan sözleşme, dosya bağımlılığı nedeniyle engellenmez. Sözleşme dosyasız aktarılır, eksik dosya ayrıca raporlanır; yeni CDN altyapısı veya sahte dosya kaydı oluşturulmaz.

- Tahsilat modülünün HTTP sözleşmesinde salt-okuma uçları `GET`, durum değiştiren bütün uçlar `POST` kullanır. Fiziksel silme semantiği URL'deki `/delete`, güncelleme semantiği `/update` eylemiyle açık tutulur; `PUT`, `PATCH` ve `DELETE` kullanılmaz. Kullanıcıya gösterilen tahsilat başarı/hata/uyarıları mevcut ortak `toast.push` altyapısından ve Türkçe metinle verilir.

- Mevcut Program IRepository kaydı ActivatorUtilities ile ayrı AppDataContext üretir. Inject edilen UnitOfWork ve başka inject context'i aynı transaction sanmak hatalı olur. Ortak global DI değiştirilmeden transaction primitive'i mevcut Repository sınıfını transaction'ın context'iyle oluşturur; aynı-context yazılar korunur. Bu desen mevcut Helpdesk doğrudan context transaction kullanımına uygundur. Yeni genel repository/transaction framework kurulmadı. Temiz/dedicated context zorunludur, ambient veya tracked kullanıcı değişiklikleriyle çalışmaz.

- 10 Eylül revizyonu: aşağıdaki ve Completed bölümündeki banka/kart korunması kayıtları tarihsel çalışma kaydıdır, artık aktif gereksinim değildir. P02 banka canonical map/seed işi mevcut kart kapsamıyla birlikte iptal edildi. “Yeni başlangıç gerçek gün” yalnız imza tarihinin korunmasını ifade eder; imza günü ilk borç tarihi olarak kullanılamaz. İlk borç kuralı 15'i kesme açıklamasına göre ayrıca netleştirilecek. Eski 18 numaralı karar belgesi yeni müşteri yanıtının önüne geçmez.

- PaymentOperation yalnız başarıyla commit edilen işlem makbuzudur; ödeme mutation ile aynı transaction'da yazılmalıdır. Paralel aynı anahtar yarışında unique ihlali sonrası transaction geri alınır ve receipt yeniden okunur; henüz uygulanmadı. PayloadHash gelecekte operation kind, bütün finansal alanlar, expected rowversion ve canonical şema sürümünü kapsamalı; istemciden hazır hash kabul edilmez. Typed before/after snapshot yalnız ödeme alanlarını içerecek; credential/kart verisi/log ham request tutulmaz. Audit retention ve silinmiş kaynak mapping tombstone ile birlikte davranış P12'de tamamlanacak.

- Payment ve grup takip Period alanı muhasebe ayının ilk günüdür; gerçek yenileme günü değildir. Aynı ay içinde reaktivasyon nedeniyle birden fazla geçerli tahakkuk olursa aylık görünüm bunları toplar; bu durum ayrı döneme otomatik ödeme dağıtımı getirmez. Tahakkuk DueDate bilgisi ayrı tutulacak, migration ham ay/yılını kaybetmeyecek. Para birimi uyuşmayan aylık kayıtlar birleştirilmeyecek.
- Fiziksel silmeye uygun entity, güvenli silme iş akışı tamamlandı demek değildir. Audit, idempotency tombstone ve transaction P03/P07 tamamlanmadan Payment generic CRUD'a veya aktif EF modele açılmaz.

- FE denetimi: parseApiError içindeki data.message dalı network/timeout çevirisinden önce; Türkçe gereksinimi için ortak helper'da dar kapsamlı ve regresyon testli düzeltme P15'e eklendi. DataTable local sort ile store sorgusu remount sonrası ayrışabilir; geriye uyumlu controlled-sort desteği P08 kabulüne dahil. Aynı sorgu mutate sırasında isValidating kullanılmalı. AuthProvider zaten SWR cache temizliyor, ikinci temizleyici eklenmeyecek.
- P01 BillingBehavior enum'u Model katmanına taşındı; hesap ve persistence aynı enum'u kullanıyor. Aktif dönemlerde unique ContractId/EffectiveFrom ve tek açık dönem indeksleri var; bunlar kapalı aralık çakışmalarını tek başına engellemez. Aralık kontrolü ve transaction P05 kapsamındadır.

- Güncel muhasebe kararı 18 belgesidir; eski ayın ilk günü zorunluluğu yeni kayıtlar için geçersiz. Geçmiş verinin gün hassasiyeti ayrı korunur.
- ORM metadata/ToQueryString testleri DB constraint veya SQL performans kanıtı değildir. 503 model guard fiziksel tablo varlığını kanıtlamaz.
- Generic CRUD yazma action'ları nedeniyle read controller'da devralınmaz; ortak ResponseModel/MenuAuthorize/DI korunur.
- Güncel denetimde iki repo origin/tahsilat-module izliyor, eski dosyaların çoğu commit edilmiş; mevcut yerel değişiklikler korunacak. Önceki “hiç commit yok” notları tarihsel kayıttır. Bu agent bu tur commit/push/merge yapmadı.
- Eksik alanların şimdilik modelde olmaması kaynak verisini atma kararı değildir. Kullanılabilir olmayan endpoint default kapalı kalır; mock veri gerçek veri gibi gösterilmez.

# Risks / Open Questions

- Legacy MGS veritabanının artık aktif kullanılmadığı kullanıcı tarafından 19 Eylül'de doğrulandı. Bu nedenle kaynak ayarı değiştirmeden, yalnız SELECT kullanan tek Serializable transaction ile doğrudan kesit alınabilir. Üretilecek kişisel veri içeren dosyalar Git dışında tutulur; hedef yazma yalnız AssistFlowTest staging ile sınırlıdır.

- Sözleşme dosyaları mevcut CDN'in herkese açık bağlantı modelini kullanır; kullanıcı ortak altyapıyı değiştirmeden kullanma kararı verdi. Dosya bağlantısı paylaşıldığında erişilebilir; farklı erişim politikası gerekirse ayrıca kararlaştırılmalıdır.

- Müşteriden satır bazlı cevap bekleyen veri kararları: müşteri türü uyuşmazlıkları (madde 2), AssistFlow'da bulunamayan müşteriler (madde 3), null işlem türleri (madde 5), tarife çakışması/güncel tutar farkları (madde 6) ve para birimi/tutar istisnaları (madde 7). Dönüş alınana kadar bu satırlar karantinada kalır; otomatik müşteri oluşturulmaz, tür/tarife/tutar tahmin edilmez.

- AssistFlow tahsilat tablolarının kurulumu artık yetkili; hedef bağlantı ve mevcut şema doğrulaması uygulama önkoşuludur. Production geçişi, gerçek veri aktarımı ve ortak tablo değişiklikleri ayrıca onaylı olmalıdır.
- GBP veya ortak Customer/ServiceType/CustomerType alan/veri değişiklikleri kullanıcı onayına tabidir; mevcut genel özerklik bunu kaldırmaz.
- AsOfDate işlem tarihi ile kayıt zamanı ayrımı ve geçmiş rapor anlamı net değil. Canlı finansal rapor açılmadan gerçek senaryoyla karar gerekir.
- Legacy gerçek başlangıç günü ve dondurma geçmişi eksik olabilir; bugünkü durumdan geçmiş uydurulmaz. Currency adaylarında belirsizlik var; tek aday aktarım onayı değildir.
- Banka alanlarının korunması ve dönem içi zam/aktifleşme yorumları çalışma kararıdır; muhasebeye iletilecek not 18'de, değiştirilirse etkili tarih/göç değerlendirilir.
- FE tam type-check, görsel kabul ve otomatik kullanıcı akışı henüz tamamlanmadı. Mevcut bağımlılık/build uyarıları ayrı sınıflandırılmalı; ilgisiz toplu refactoring yapılmaz.
- Tüm P maddeleri bitmeden “production hazır” denmeyecek. Riskli işlemi engelleyen sınır, bağımsız güvenli işlerin durması anlamına gelmez.
